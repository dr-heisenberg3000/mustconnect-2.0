package com.must.connect.ui.theme

import androidx.compose.ui.graphics.Color

// ── MUST-CONNECT Brand Palette ──────────────────────────────────────────────

// Splash gradient colours (deep navy → vivid royal blue)
val BrandNavy       = Color(0xFF0D1B6E)  // darkest, gradient start
val BrandRoyalBlue  = Color(0xFF1A3BB5)  // mid gradient
val BrandBlue       = Color(0xFF2351D8)  // gradient end / primary

// Light-theme surface / accent
val BrandDarkNavy   = Color(0xFF0A1552)  // text on white for role cards
val AccentBlue      = Color(0xFF1A3BB5)  // icon tint, selected state
val SurfaceCard     = Color(0xFFFFFFFF)
val BackgroundLight = Color(0xFFF5F7FA)
val SubtleGrey      = Color(0xFFF0F2F5)  // card icon chip background
val TextPrimary     = Color(0xFF0D1540)
val TextSecondary   = Color(0xFF5C6680)
val DividerColor    = Color(0xFFE2E6EF)

// Splash text / indicators
val OnBrandWhite    = Color(0xFFFFFFFF)
val OnBrandWhite70  = Color(0xB3FFFFFF)  // 70% white

// Material scheme seeds
val PrimaryLight    = BrandBlue
val OnPrimaryLight  = Color.White
val PrimaryContainer= Color(0xFFD8E2FF)
val SecondaryLight  = Color(0xFF4A5FA3)
val TertiaryLight   = Color(0xFF1565C0)